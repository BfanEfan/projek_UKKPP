<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Data Siswa</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <header class="bg-black text-white px-6 py-4 flex justify-between">
    <h1 class="font-bold">Data Siswa</h1>
    <a href="/dashboard" class="hover:underline">Dashboard</a>
  </header>

  <main class="p-6">
  <form action="/dasis" method="GET" class="mb-4 flex gap-2 justify-end">
    <input 
      type="text" 
      name="keyword"
      value="<%= keyword %>"
      placeholder="Cari nama / NIS siswa..."
      class="px-4 py-2 border rounded w-64 focus:outline-none focus:ring"
    >
    <button 
      type="submit" 
      class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded">
      Cari
    </button>
  </form>
    <table class="w-full bg-white rounded shadow">
      <thead class="bg-gray-200">
        <tr>
          <th class="p-2 text-left">NIPD</th>
          <th class="p-2 text-left">Siswa</th>
          <th class="p-2 text-left">Kelas</th>
          <th class="p-2 text-left">Jurusan</th>
          <th class="p-2 text-left">Total-poin</th>
        </tr>
      </thead>
      <tbody>
        <% siswa.forEach(s => { %>
          <tr class="border-t">
            <td class="p-2"><%= s.nipd %></td>
            <td class="p-2"><%= s.nama_siswa %></td>
            <td class="p-2"><%= s.kelas %></td>
            <td class="p-2"><%= s.jurusan %></td>
            <td class="p-2"><%= s.total_poin %></td>
            
          </tr>
        <% }) %>
      </tbody>
    </table>
  </main>

</body>
</html>
